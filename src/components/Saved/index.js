import React from "react";
import styled from "styled-components";

const Saved = () => {
	return <Wrapper>Saved</Wrapper>;
};

const Wrapper = styled.div`
	grid-area: saved;
	display: grid;
	place-items: center;
`;

export default Saved;
